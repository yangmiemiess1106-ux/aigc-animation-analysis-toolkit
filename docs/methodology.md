# Methodology

Each animation is treated as a case. A case record stores provenance and rights information alongside observations. The current CSV schema is intentionally small so a researcher can inspect it in a spreadsheet.

Preserve the source URL and access date, distinguish observation from interpretation, and record uncertainty in `confidence`. Keep raw media and sensitive material outside the repository.
